/**
 * Boundary for future HTTP / Firebase — import types from here in UI code
 * so swapping persistence is one module change.
 */
export type {
  FilmDataState,
  Title,
  PremiereEvent,
  User,
  FeatureFlags,
} from "../domain/types";
